const constants = {
  error: {
    INVALID_FILE_LENGTH: 'The file exceeds the maximum size of 4 lines.',
    INVALID_FILE_FIELDS: 'The provided properties are invalid.',
  }
};

module.exports = {
  constants
}